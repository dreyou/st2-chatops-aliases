# refers to `ansible` st2 integration pack:
# https://github.com/StackStorm/st2contrib/tree/master/packs/ansible
---
name: "chatops.ansible"
action_ref: "ansible.command"
description: "Run ansible command, ex: !ansible hosts=hostname module-name=ping args=''"
formats:
  - "ansible {{args}}"
